# XML Fast Infoset decoder for Burp Suite

NOTE: It can only works on Sun JRE, since private Java package was used.

# Usage
```
$ cd albatross
$ mvn package
```

open Burp Suite, switch to "Extender" tab, add the target jar as extension